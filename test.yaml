version: "3.3"
services:
  test:
    container_name: test
    environment:
      tz: Asia/Shanghai
    image: test:latest
    networks:
    - gin
    ports:
    - 80:80
    volumes:
    - /opt/test:/opt/test
